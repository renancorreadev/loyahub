version: "3.8"

services:
  postgres:
    image: postgres:16-alpine
    container_name: loyahub-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: indexer
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  rabbitmq:
    image: rabbitmq:3.12-management-alpine
    container_name: loyahub-rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "check_port_connectivity"]
      interval: 10s
      timeout: 5s
      retries: 5

  indexer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: loyahub-indexer
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    environment:
      RPC_URL: ${RPC_URL:-http://host.docker.internal:8545}
      WS_URL: ${WS_URL:-ws://host.docker.internal:8546}
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: postgres
      DB_PASSWORD: postgres
      DB_NAME: indexer
      RABBITMQ_URL: amqp://guest:guest@rabbitmq:5672/
      API_PORT: 8080
      CONFIRMATIONS_REQUIRED: 12
      BLOCKS_PER_BATCH: 100
      MAX_WORKERS: 5
      LOG_LEVEL: info
    ports:
      - "8080:8080"
    restart: unless-stopped

volumes:
  postgres_data:
  rabbitmq_data:
